{
    "server": true,
    "datacenter": "{{ consul.datacenter }}",
    "data_dir": "{{ consul.data_dir }}",
    "encrypt": "{{ consul.encrypt }}",
    "log_level": "INFO",
    "enable_syslog": true
}
